<Application x:Class="ZachetAcademyApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ZachetAcademyApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <Style TargetType="StackPanel" x:Key="StpCenter">

            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>

        </Style>

        <Style TargetType="Button" x:Key="BtnDefault">

            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="200px"/>
            <Setter Property="Height" Value="50px"/>
            <Setter Property="FontSize" Value="15pt"/>

        </Style>

        <Style TargetType="ComboBox" x:Key="CmbDefault">

            <Setter Property="Margin" Value="10 10 0 0"/>
            <Setter Property="FontSize" Value="15pt"/>
            <Setter Property="Width" Value="150px"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>

        </Style>
        
    </Application.Resources>

</Application>
